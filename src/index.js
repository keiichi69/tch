import React from 'react';
import ReactDOM from 'react-dom/client';
import QuizAppFinal from './QuizAppFinal';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <QuizAppFinal />
  </React.StrictMode>
);
